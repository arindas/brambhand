# brambhand (Python package)

This directory contains the Python simulation/runtime package.

- Source: `src/brambhand/`
- Tests: `tests/`
- Packaging config: `pyproject.toml`

For project-wide architecture, roadmap, and docs, see the repository root `README.md` and `docs/`.
